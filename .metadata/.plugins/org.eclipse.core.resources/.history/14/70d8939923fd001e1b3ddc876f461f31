<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smhrd.db.BoardMapper">

<!-- jsp/서블릿 할때 사용했던 mybatis에서의 namespace
: 단순한 식별자 의미만 가지고 있어었음 중복만 안나게 설정하면 되었음
근데 스프링에서의 mybatis에서의 namespace 의 의미는
: 식별자 + 스프링이 sql구문을 찾을 수 있게 도와주는 경로
고로 반드시 인태페이스의 이름괴 동일하게 작성 -->


<!-- 스프링에서는 반드시 BoardMapper 인터페이스 안쪽에있는 실행해야하는 메서드명과 id 값을 일치해야한다 -->
<select id="boardList" resultType="com.smhrd.model.BoardVO">
select * from board  
</select>
<insert id="register" parameterType="com.smhrd.model.BoardVO">
    insert into board(title,writer,content)
    values(#{title},#{writer},#{content})
    <!-- values('${title}','${writer}','${content}') -->
    
 </insert>




















</mapper>